<h2>New comment</h2>

<%= render "form.html", changeset: @changeset,
                        action: "/posts/1/comment"
                        %>

<%= link "Back", to: comment_path(@conn, :index) %>

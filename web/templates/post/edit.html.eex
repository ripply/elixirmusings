<h2>Edit post</h2>

<%= render "form_no_beer.html", changeset: @changeset,
    beers: @beers,
    beer_id: @beer_id,
    action: post_path(@conn, :update, @post) %>

<%= link "Back", to: post_path(@conn, :index) %>
